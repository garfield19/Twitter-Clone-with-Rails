<div class="panel panel-default post-form-panel">
  			<div class="panel-body row">
  				<div class="col-md-1">
  					 avatar
  				</div>
  				<div class="col-md-11">
					  <%= form_for(@newUpdate) do |f| %>
					<div class="field" style="margin-left: 10px;">
					    <%= f.text_field :content, autofocus: true, class: "form-control" %>
					     <span type="button" class="btn btn-submit" style="float: right; margin-top: 3px;"><%= f.submit "Update", class: "btn btn-primary" %></span>
					  </div>
					
					<% end %>
  				</div>
  						
			</div>
		</div>

