<%= form_for(@newUpdate) do |f| %>
 <center>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content, autofocus: true %>
  </div>
<div class = "actions">
  <%= f.submit "Update" %>
</div>
<center>
<% end %>
