
<div class="row">
	<center><h3><div class="panel-heading">All conversations</div></h3>
	<div class="col-md-8 col-md-offset-2">
		<%= link_to "New Conversation", new_conversation_path, :class => 'btn btn-success' %>
		<%= link_to "Back home", home_path, :class => 'btn btn-warning' %>
		<p><p>

<% @conversations.each do |conversation| %>
	<div>
		<%= link_to conversation.subject, conversation_path(conversation) , :class => 'btn btn-danger btn-block'%>
		<p>
	</div>
<% end %>
</div></center>